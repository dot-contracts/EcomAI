namespace EcomVideoAI.Application.DTOs.Requests.Auth
{
    public class GetCurrentUserRequest
    {
        // This is typically an empty request since user info comes from JWT token
        // but we need it for the interface pattern
    }
} 